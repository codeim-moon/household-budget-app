# 가계부 앱 PRD (Household Budget App)

> 이 문서는 제품 요구사항의 **단일 기준(Single Source of Truth)** 이다.
> 구현 중 판단이 갈리는 지점은 이 문서를 우선한다.

## 1. 제품 개요

개인과 가족이 함께 쓰는 월 단위 가계부 웹 앱. 수입·지출을 기록하고, 이번 달 남은 돈을
한눈에 확인하며, 이후 예산·반복 거래·분석으로 확장한다.

- 대상: 한국어 사용자, 원화(KRW) 기준
- 형태: 모바일 우선 반응형 웹 (데스크톱 대응)
- 인증: 이메일 회원가입/로그인 (Supabase Auth)
- 데이터: Supabase(PostgreSQL). **서버 데이터가 최종 원본이다.**

## 2. 단계별 범위

| 단계 | 범위 |
| --- | --- |
| **1단계 (현재)** | 프로젝트 기반, DB 스키마 전체, 인증·RLS, 온보딩 기본 데이터, 앱 셸, 홈 요약, 거래 목록, 거래 등록/수정/삭제 |
| 2단계 | 예산(전체/카테고리), 반복 거래 생성·전개, 분석 화면 |
| 3단계 | 설정 상세(멤버·카테고리 관리), 데이터 내보내기, 고급 분석 |

1단계에서 **스키마는 2·3단계 항목(budgets, recurring_rules, user_settings)까지 함께 준비**하되,
UI/기능은 거래 중심으로만 구현한다.

## 3. 사용자 스토리 (1단계)

- 사용자는 이메일로 가입하고 로그인/로그아웃할 수 있다.
- 새로고침해도 로그인 상태가 유지되고, 비로그인 상태로 앱 화면에 접근할 수 없다.
- 가입 즉시 `본인` 거래 주체와 기본 카테고리가 준비되어 바로 기록을 시작할 수 있다.
- 홈에서 이번 달 남은 돈, 총수입, 총지출, 최근 거래 5건을 확인한다.
- 월을 앞뒤로 이동하고, 특정 사용자(멤버)로 필터링한다.
- 거래를 등록·수정·삭제하고, 삭제는 실수해도 되돌릴 수 있다.

## 4. 데이터 모델

모든 사용자 데이터 테이블은 `owner_id`(= 로그인 계정)를 가진다.

| 테이블 | 설명 |
| --- | --- |
| `profiles` | 로그인 계정 정보. `profiles.id = auth.users.id` |
| `members` | 계정 내부 거래 주체 (본인·가족·공용 등) |
| `category_groups` | 중분류. 수입: 고정수입/비정기수입, 지출: 고정지출/개인지출 |
| `categories` | 소분류. `kind`로 `consumption`(소비) / `asset_transfer`(자산 이동) 구분 |
| `transactions` | 거래. 유형·금액·거래일·멤버·카테고리·내용·메모·상태 |
| `budgets` | 월 예산. 전체 예산(`category_id is null`) 또는 카테고리 예산 |
| `recurring_rules` | 반복 거래 규칙 |
| `user_settings` | 사용자별 앱 설정 (예정 거래 포함 여부 등) |

### 기본 카테고리 (온보딩 시 자동 생성)

| 유형 | 중분류 | 소분류 |
| --- | --- | --- |
| 수입 | 고정수입 | 급여 |
| 수입 | 비정기수입 | 부수입 |
| 지출 | 고정지출 | 주거, 통신, 구독, 저축*, 투자* |
| 지출 | 개인지출 | 식비, 교통, 쇼핑, 의료, 기타 |

`*` 표시는 `kind = asset_transfer` (실제 소비가 아닌 자산 이동)

### 제약 사항 (데이터베이스 수준)

- 금액은 **1 이상의 정수**(원 단위)만 허용한다.
- 거래에 연결되는 `member`, `category`는 **반드시 같은 `owner_id`** 여야 한다.
  → 복합 외래키 `(member_id, owner_id)`, `(category_id, owner_id, transaction_type)` 로 방어한다.
- 카테고리의 `transaction_type`은 항상 소속 중분류의 `transaction_type`과 일치한다.
- 같은 월의 전체 예산은 1개, 같은 월·카테고리 예산도 1개만 존재한다.
- 반복 거래는 `(recurring_rule_id, occurrence_month)` 조합이 중복될 수 없다.
- 모든 주요 테이블은 `created_at`, `updated_at`을 가지며 `updated_at`은 자동 갱신된다.

## 5. 보안 요구사항

- 모든 사용자 데이터 테이블에 **RLS 활성화**.
- `SELECT / INSERT / UPDATE / DELETE` 각각에 대해 `owner_id = auth.uid()` 정책을 둔다.
- 클라이언트가 다른 `owner_id`를 보내도 절대 접근/생성할 수 없어야 한다.
- 브라우저에는 **anon key만** 사용한다. `service_role` key는 프런트엔드 어디에도 두지 않는다.
- 신규 사용자 기본 데이터 생성은 **중복 없이(멱등적으로)** 동작해야 한다.

## 6. 기능 요구사항 (1단계)

### 6.1 인증
- 이메일 회원가입 / 로그인 / 로그아웃
- 세션 복구(새로고침 유지), 보호 라우트

### 6.2 앱 셸
- 모바일: 하단 탭 / 데스크톱: 왼쪽 사이드바
- 메뉴: 홈, 내역, 추가, 분석, 설정
- 분석·설정의 미구현 영역은 "준비 중"임을 명확히 표시

### 6.3 홈
- 현재 월 선택기 (이전/다음 월 이동)
- 전체 / 사용자(멤버) 프로필 필터
- **이번 달 남은 돈 = 완료된 총수입 − 완료된 총지출**
- 총수입, 총지출 표시
- "예정 거래 포함" 토글이 켜지면 예정 거래를 포함한 값을 **별도로 명확히** 표시
- 최근 거래 5건

### 6.4 거래 목록(내역)
- 월 이동, 최신순 기본 정렬
- 수입/지출을 색과 부호로 명확히 구분

### 6.5 거래 등록 / 수정 / 삭제
- 필수값: 유형(수입/지출), 1원 이상 금액, 거래일, 멤버, 중분류에 맞는 소분류, 상태(완료/예정)
- 선택값: 내용, 메모
- 유형 변경 시 선택 가능한 중분류·소분류가 즉시 올바르게 바뀐다
- 저장 중 버튼 비활성화(중복 제출 방지), 성공 시 홈 합계·목록 즉시 갱신
- 삭제 전 확인 → 삭제 후 **실행 취소** 제공 (소프트 삭제 방식)

### 6.6 상태 표시
- 로딩 / 빈 상태 / 네트워크 오류 / 유효성 오류를 각각 **이해하기 쉬운 한국어**로 보여준다

## 7. 디자인 원칙

> "토스를 더 미니멀하게 해석한 디자인" — 토스 화면이나 브랜드 자산을 그대로 복제하지 않는다.

- 배경: 흰색 / 옅은 회색
- 텍스트: 짙은 네이비
- 포인트 컬러: **블루 한 가지만**
- 넉넉한 여백, 큰 핵심 금액, 16~20px 라운드 카드
- 모바일 360px에서 가로 스크롤 없음
- 터치 영역 최소 44 × 44px

## 8. 기술 스택

- Vite + React + TypeScript
- Tailwind CSS
- React Router
- `@supabase/supabase-js`
- date-fns (날짜), lucide-react (아이콘)
- Vitest + Testing Library (테스트)

## 9. 저장 정책

- 거래 원본 데이터를 `localStorage`에 저장하지 않는다.
- `localStorage`는 마지막 선택 월, 필터 같은 **UI 설정에만** 사용한다.

## 10. 완료 기준 (1단계)

- [ ] typecheck / lint / test / production build 모두 통과
- [ ] 핵심 금액 계산·거래 폼 유효성 자동 테스트 존재
- [ ] 비인증 접근과 타 `owner_id` 접근이 RLS로 차단됨을 확인할 절차 문서화
- [ ] README에 설치·환경 변수·마이그레이션·실행 방법 기록
- [ ] `docs/implementation-status.md`에 완료/미완료/알려진 문제 기록
